use auth {
  providers = [local]

  tenant = true

  claims = {
    role = enum["owner", "admin", "member"]
    plan = enum["free", "pro"]
    billing = enum["none", "manager"]
  }

  agents = true
}
