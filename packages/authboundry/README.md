# authboundry

This package preserves `npx authboundry` and redirects JavaScript consumers to
the canonical package, [`@authboundry/core`](https://www.npmjs.com/package/@authboundry/core).

New installations should use:

```sh
npm install @authboundry/core
npx authboundry --help
```
